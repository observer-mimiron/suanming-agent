
# M0.1: Go 项目初始化

**目标：** Go module + Gin + SSE Writer 骨架可用  
**时间：** 10min  
**依赖：** 无

---

## 步骤

- [ ] 初始化 Go module

```bash
cd /Users/wikiglobal/workSapce/suanming-agent
go mod init github.com/wikiglobal/suanming-agent
go get github.com/gin-gonic/gin
```

- [ ] 创建入口 `cmd/server/main.go`

```go
package main

import (
    "log"
    "github.com/gin-gonic/gin"
)

func main() {
    r := gin.Default()
    r.GET("/api/health", func(c *gin.Context) {
        c.JSON(200, gin.H{"status": "ok"})
    })
    r.POST("/api/chat", func(c *gin.Context) {
        c.JSON(200, gin.H{"status": "not implemented"})
    })
    log.Println("命理大师 :8080")
    r.Run(":8080")
}
```

- [ ] 创建 SSE Writer `internal/sse/writer.go`

```go
package sse

import (
    "encoding/json"
    "fmt"
    "github.com/gin-gonic/gin"
)

type Writer struct{ c *gin.Context }

func NewWriter(c *gin.Context) *Writer {
    c.Header("Content-Type", "text/event-stream")
    c.Header("Cache-Control", "no-cache")
    c.Header("Connection", "keep-alive")
    c.Header("Access-Control-Allow-Origin", "*")
    return &Writer{c: c}
}

func (w *Writer) Send(eventType string, data any) error {
    jsonData, _ := json.Marshal(data)
    _, err := fmt.Fprintf(w.c.Writer, "event: %s\ndata: %s\n\n", eventType, jsonData)
    w.c.Writer.Flush()
    return err
}
```

## 验收

- [ ] `go build ./cmd/server/` 编译成功
- [ ] `go run ./cmd/server/` 后 `curl localhost:8080/api/health` 返回 `{"status":"ok"}`
