# M5.6: App.vue 暗色主题 + 编译验证

**目标：** 完整前端编译通过，暗色聊天界面可用  
**时间：** 10min  
**依赖：** M5.5

---

## 步骤

- [ ] 重写 `web/src/App.vue`

```vue
<template>
  <n-config-provider :theme="darkTheme">
    <n-message-provider><ChatPanel/></n-message-provider>
  </n-config-provider>
</template>
<script setup lang="ts">
import {darkTheme,NConfigProvider,NMessageProvider} from 'naive-ui'
import ChatPanel from './components/ChatPanel.vue'
</script>
```

- [ ] 类型检查 + 构建

```bash
cd web && npx vue-tsc --noEmit && npm run build
```

## 验收

- [ ] 零类型错误
- [ ] npm run build 成功
- [ ] `npm run dev` 后浏览器显示暗色聊天界面，输入框可用
