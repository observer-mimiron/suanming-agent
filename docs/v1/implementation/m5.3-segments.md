# M5.3: TextSegment + ThinkingSegment + ToolCallSegment

**目标：** 3 个基础 Segment 组件可用  
**时间：** 15min  
**依赖：** M5.2

---

## 步骤

- [ ] `web/src/components/TextSegment.vue`

```vue
<template><p class="text">{{ content }}</p></template>
<script setup lang="ts">defineProps<{content:string}>()</script>
<style scoped>.text{margin:0;line-height:1.8;color:#E8E1D5;white-space:pre-wrap}</style>
```

- [ ] `web/src/components/ThinkingSegment.vue`

```vue
<template>
  <n-collapse><n-collapse-item title="🧠 Agent 思考中...">
    <n-tag size="small" :bordered="false">{{ agent }}</n-tag>
    <p>{{ text }}</p>
  </n-collapse-item></n-collapse>
</template>
<script setup lang="ts">import {NCollapse,NCollapseItem,NTag} from 'naive-ui'; defineProps<{text:string;agent:string}>()</script>
```

- [ ] `web/src/components/ToolCallSegment.vue`

```vue
<template><n-tag type="warning" size="small">🔧 {{ tool }}</n-tag></template>
<script setup lang="ts">import {NTag} from 'naive-ui'; defineProps<{tool:string}>()</script>
```

## 验收

- [ ] `npx vue-tsc --noEmit` 无错误
