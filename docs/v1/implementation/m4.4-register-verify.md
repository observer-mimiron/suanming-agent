# M4.4: 注册确定性 Tool + curl 验证

**目标：** 注册确定性 Tool，完整排盘流程可走通  
**时间：** 10min  
**依赖：** M4.3

---

## 步骤

- [ ] 更新 main.go 注册确定性 Tool

```go
reg.Register(&tools.BaziCalcTool{})
reg.Register(tools.NewKnowledgeSearchTool())
```

- [ ] Orchestrator 内部直接调用 `llm.NewClient()`，不注册 `llm_generate` Tool

- [ ] curl 完整流程测试

```bash
go run ./cmd/server/ &

# 完整排盘
curl -N -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"1990年5月20日早上8点，男。今年事业怎么样？","session_id":"t1"}'
```

## 验收

- [ ] SSE 流含: thinking → tool_call(bazi_calc) → component(bazi-chart) → tool_call(knowledge_search) → component(knowledge-sources) → text(流式) → done
- [ ] 流式文本逐字推送
- [ ] 知识库不可用时 passages 为空，不阻塞回答
