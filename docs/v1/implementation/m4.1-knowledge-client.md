# M4.1: MCP KnowledgeClient + knowledge_search Tool

**目标：** 通过项目知识库 MCP 检索命理资料  
**时间：** 15min  
**依赖：** M3.6

---

## 步骤

- [ ] 创建 `internal/mcp/client.go`

```go
package mcp

import (
    "bytes" "encoding/json" "fmt" "net/http" "time"
)

type Client struct {
    baseURL string
    client  *http.Client
}

func NewClient(baseURL string) *Client {
    return &Client{baseURL: baseURL, client: &http.Client{Timeout: 5 * time.Second}}
}

type Passage struct {
    Content string `json:"content"`
    Source  string `json:"source"`
}

func (c *Client) Search(query string, topK int) ([]Passage, error) {
    body := map[string]any{"query": query, "limit": topK}
    jsonBody, _ := json.Marshal(body)
    resp, err := c.client.Post(c.baseURL+"/search", "application/json", bytes.NewReader(jsonBody))
    if err != nil { return nil, fmt.Errorf("rag search: %w", err) }
    defer resp.Body.Close()
    var result struct{ Passages []Passage `json:"passages"` }
    if err := json.NewDecoder(resp.Body).Decode(&result); err != nil { return nil, err }
    return result.Passages, nil
}
```

- [ ] 创建 `internal/tools/knowledge_search.go`

```go
package tools

import (
    "fmt"
    "os"
    "github.com/wikiglobal/suanming-agent/internal/mcp"
)

type KnowledgeSearchTool struct{ client *mcp.Client }

func NewKnowledgeSearchTool() *KnowledgeSearchTool {
    url := os.Getenv("KNOWLEDGE_MCP_URL")
    if url == "" { url = "http://localhost:3001" }
    return &KnowledgeSearchTool{client: mcp.NewClient(url)}
}

func (t *KnowledgeSearchTool) Name() string        { return "knowledge_search" }
func (t *KnowledgeSearchTool) Description() string { return "检索项目知识库中的命理资料" }

func (t *KnowledgeSearchTool) Execute(params map[string]any) (any, error) {
    query, ok := params["query"].(string)
    if !ok || query == "" {
        return nil, fmt.Errorf("query is required")
    }
    topK := 3
    if v, ok := params["topK"].(float64); ok { topK = int(v) }
    passages, err := t.client.Search(query, topK)
    if err != nil {
        return map[string]any{"passages": []mcp.Passage{}, "fallback": true}, nil
    }
    return map[string]any{"passages": passages}, nil
}
```

## 验收

- [ ] `go build ./internal/mcp/ ./internal/tools/` 编译成功
- [ ] KnowledgeSearchTool 注册后 M4.4 验证
