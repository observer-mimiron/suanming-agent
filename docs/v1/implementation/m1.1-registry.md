# M1.1: 工具注册中心 + lunar-go 安装

**目标：** Tool Registry 可用，lunar-go 依赖安装完成  
**时间：** 20min  
**依赖：** M0.3

---

## 步骤

- [ ] 安装 lunar-go

```bash
cd /Users/wikiglobal/workSapce/suanming-agent
go get github.com/6tail/lunar-go
```

- [ ] 创建 `internal/tools/registry.go`

```go
package tools

import "sync"

type Tool interface {
    Name() string
    Description() string
    Execute(params map[string]any) (any, error)
}

type Registry struct {
    mu    sync.RWMutex
    tools map[string]Tool
}

func NewRegistry() *Registry {
    return &Registry{tools: make(map[string]Tool)}
}

func (r *Registry) Register(t Tool) {
    r.mu.Lock()
    defer r.mu.Unlock()
    r.tools[t.Name()] = t
}

func (r *Registry) Get(name string) (Tool, bool) {
    r.mu.RLock()
    defer r.mu.RUnlock()
    t, ok := r.tools[name]
    return t, ok
}

func (r *Registry) List() []Tool {
    r.mu.RLock()
    defer r.mu.RUnlock()
    list := make([]Tool, 0, len(r.tools))
    for _, t := range r.tools {
        list = append(list, t)
    }
    return list
}
```

## 验收

- [ ] `go build ./internal/tools/` 编译成功
- [ ] `go doc github.com/6tail/lunar-go/calendar` 可查看 lunar 文档
