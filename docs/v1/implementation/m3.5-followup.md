# M3.5: handleFollowupReading 复用命盘

**目标：** 排盘后追问时命中“复用已有命盘”分支，M3 先用占位响应验证状态机  
**时间：** 10min  
**依赖：** M3.4

---

## 步骤

- [ ] 在 orchestrator.go 添加

```go
func (o *Orchestrator) handleFollowupReading(sw *sse.Writer, st *state.SessionState) error {
    sw.Send("thinking", map[string]any{
        "agent":"orchestrator","text":"复用已有命盘，正在回答你的问题...",
    })
    sw.Send("text", map[string]any{
        "content":"命盘已就绪。M4 接入知识检索和 LLM 后，这里会给出真正的咨询回答。",
    })
    sw.Send("done", map[string]any{})
    return nil
}
```

## 验收

- [ ] 编译成功
- [ ] M3.6 测试验证追问不复算，且能命中 followup 分支
