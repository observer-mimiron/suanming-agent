# M0.3: 验证编译

**目标：** Go 和 Vue 两端编译 + 启动均正常  
**时间：** 5min  
**依赖：** M0.1, M0.2

---

## 步骤

- [ ] Go 编译

```bash
cd /Users/wikiglobal/workSapce/suanming-agent
go build ./cmd/server/
```

- [ ] Go 启动 + 健康检查

```bash
go run ./cmd/server/ &
sleep 1
curl http://localhost:8080/api/health
# → {"status":"ok"}
```

- [ ] Vue 类型检查 + 构建

```bash
cd web
npx vue-tsc --noEmit && npm run build
```

## 验收

- [ ] Go: `{"status":"ok"}`
- [ ] Vue: 构建成功，无类型错误
